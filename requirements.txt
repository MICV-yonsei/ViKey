# Core deep learning
torch>=2.1.0
torchvision
transformers>=4.40.0
accelerate

# Video / image
decord
Pillow
numpy
matplotlib

# Utilities
tqdm

# Keyword extraction (Qwen2.5-7B-Instruct via vLLM)
vllm>=0.5.0

# LLaVA-NeXT (LLaVA-Video) — install from source
# pip install git+https://github.com/LLaVA-VL/LLaVA-NeXT.git

# OpenAI CLIP — install from source
# pip install git+https://github.com/openai/CLIP.git
